version: "3"
services:
  ticksim:
    build:
      context: .
      dockerfile: Dockerfile
    image: ticksim_Image
    volumes:
      - /vveeo_apps/release/ui:/usr/share/nginx/html/
    networks:
      - ticksim_ui_network
    ports:
      - "3000:80"
networks:
  ticksim_ui_network:
